Fellowship
==========

This project involves people interaction on a website
=======

This project involves people interaction on a website
It's an implementation of the MongoDB tutorial "TumbleLog"

Installation
============

On your system : 
> sudo apt-get install python-virtualenv
> easy_install -U distribute
> sudo apt-get install libmysqlclient-dev

Use virtualenv to install package via pip using the requirements.txt file:
> pip install -r fellowship/requirements.txt

Dependencies
============
Setting last version of bootstrap
=================================

> python scripts/setall.py --archive true --bootstrap true
